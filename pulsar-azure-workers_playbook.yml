---
- name: pulsar azure worker playbook
  hosts: pulsar_azure_workers
  become: yes
  vars_files:
    - group_vars/VAULT
    - group_vars/all.yml
  roles:
    - common
    - insspb.hostname
    - galaxyproject.repos
    - galaxyproject.cvmfs

# To do:
#  - galaxyproject.slurm role and slurm configuration on pulsar-azure-0 also
#  - shared_mounts: /mnt/pulsar
#  - security groups so that this VM can communicate with others in the network.
#    Also check that it is in the same network as pulsar-azure-0
