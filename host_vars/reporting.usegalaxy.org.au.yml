telegraf_agent_output:
  - type: influxdb
    config:
      - urls = ["{{ grafana_server_url }}"]
      - database = "ancillary"
      - precision = "s"
      - username = "node"
      - password = "{{ vault_influx_node_password }}"

vector_s3_key_prefix: galaxy/nginx-logs/
