runners:
  local:
    load: galaxy.jobs.runners.local:LocalJobRunner
    workers: 4
handling:
  assign:
    - db-skip-locked
execution:
  default: local
  environments:
    local:
      runner: local
    tpv_dispatcher:
      runner: dynamic
      type: python
      function: map_tool_to_destination
      rules_module: tpv.rules
      tpv_config_files:
        - "{{ galaxy_dynamic_job_rules_dir }}/total_perspective_vortex/qaai_vortex_config.yml"
